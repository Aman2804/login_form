<table>
  <tr>
    <td><%= @user.name %> ||</td>
    <td><%= @user.email %></td>
    <td><%= link_to 'edit' , edit_user_path(@user) %></td>
  </tr>
</table>
<%= link_to 'All Profile', users_path %>
<%= link_to 'Signout', welcome_path(@user),method: :delete%>